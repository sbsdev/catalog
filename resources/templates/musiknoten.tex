\item[<%= (wrap creator "" ": " false) %>\href{<%= (to-url record-id) %>}{<%= (periodify title) %>}] <%= (render-subtitles subtitles) %> <%= (periodify name-of-part) %> - <%= (wrap source-publisher "" "" false) %><%= (wrap (year source-date) ", " "") %> \\ <%= (wrap description) %>
  <%= (wrap producer-brief) %><% (when library-signature %>\textbf{Ausleihe:} <%= (braille-signatures library-signature) %> \\ <% ) %><% (when product-number %>\textbf{Verkauf:} <%= price%>, <%= (braille-signatures product-number) %><% ) %>
