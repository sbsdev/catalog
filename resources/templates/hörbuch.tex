\item[<%= (wrap creator "" ": " false) %>\href{<%= (to-url record-id) %>}{<%= (periodify title) %>}] <%= (periodify subtitle) %> <%= (periodify name-of-part) %> - <%= (wrap source-publisher "" "" false) %><%= (wrap (year source-date) ", " "") %> \\
  <%= (wrap (translations genre) "Genre: ") %><%= (wrap description) %>
  <%= (wrap duration "" " Min., " false) %><%= (render-narrators narrators) %>
  <%= producer-brief %><%= (if produced-commercially? ", Hörbuch aus dem Handel" "") %>. \\
  \textbf{Ausleihe:} <%= library-signature %><% (when product-number %> \\
  \textbf{Verkauf:} <%= product-number%>, <%= price%><% ) %>
