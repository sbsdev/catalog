\item[<%= creator %>:] \href{<%= (to-url record-id) %>}{\textbf{<%= (periodify title) %>}} <%= (periodify subtitle) %> <%= (periodify name-of-part) %> - <%= source-publisher %>, <%= (year source-date) %>. \\
  <%= (wrap (translations genre) "Genre: ") %>
  <%= (wrap description) %>
  <%= (wrap duration "" " Min., " false) %><%= (wrap narrator "gelesen von: " "") %>
  <%= producer-brief %><%= (if produced-commercially? ", Hörbuch aus dem Handel" "") %>. \\
  \textbf{Ausleihe:} <%= library-signature %><% (when product-number %> \\
  \textbf{Verkauf:} <%= product-number%>, <%= price%><% ) %>
