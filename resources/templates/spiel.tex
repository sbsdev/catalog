\item[\href{<%= (to-url record-id) %>}{<%= (periodify title) %>}] <%= (periodify subtitle) %> <%= (periodify creator) %> \\ 
  <%= (wrap source_publisher) %>
  <%= (wrap game_category) %><%= (wrap description) %>
  <%= (wrap game_materials) %>
  \textbf{Ausleihe:} <%= library-signature %>
