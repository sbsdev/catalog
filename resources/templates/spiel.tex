\item[\href{<%= (to-url record-id) %>}{<%= (periodify title) %>}] <%= (render-subtitles subtitles) %> <%= (periodify creator) %><% (when-not (or (seq subtitles) creator) %>\ <% ) %> \\ 
  <%= (wrap source-publisher) %><%= (wrap (translations genre)) %><%= (wrap description) %><%= (wrap game-description) %>
  \textbf{Ausleihe:} <%= library-signature %>
