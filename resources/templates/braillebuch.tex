\item[<%= (wrap creator "" ": " false) %>\href{<%= (to-url record-id) %>}{<%= (periodify title) %>}] <%= (render-subtitles subtitles) %> <%= (periodify name-of-part) %> - <%= (wrap source-publisher "" "" false) %><%= (wrap (year source-date) ", " "") %> \\
  <%= (wrap (translations genre) "Genre: ") %><%= (wrap description) %>
  <%= producer-brief %><%= (if rucksackbuch? (str ", Rucksackbuch Nr. " rucksackbuch-number) "") %>. \\
  \textbf{Ausleihe:} <%= (braille-signatures library-signature) %><% (when (not-empty product-number) %> \\
  \textbf{Verkauf:} <%= price%>. <%= (braille-signatures product-number) %><% ) %>
