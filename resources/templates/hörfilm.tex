\item[\href{<%= (to-url record-id) %>}{<%= (periodify title) %>}] <%= (wrap subtitle "" "") %><% (when-not subtitle %>\ <% ) %> \\ 
  <%= (wrap directed-by "Regie: " "") %><%= (wrap actors " Schauspieler: " "") %><% (when (or directed-by actors) %> \\ <% ) %><%= (wrap movie_country) %><%= (wrap (translations genre)) %><%= (wrap description) %><%= (wrap producer) %>
  \textbf{Ausleihe:} <%= library-signature %>
